################################################################################

# This is the start of the part that has been copied from @mosbth

################################################################################
# Setting the prefix from C-b to C-a
set -g prefix C-a
# set -g default-terminal "screen-color256"
# Free the original Ctrl-b prefix keybinding
unbind-key C-b
bind-key C-a send-prefix

# colors
#setw -g mode-bg black
#set-option -g default-terminal "screen-256color" #"xterm-256color" # "screen-256color"
#set-option -g pane-active-border-fg green
#setw window-status-bg red
#set message-bg cyan
#set status-bg cyan

# Use vi or emacs
# setw -g mode-keys vi

# Select, copy and paste text in tmux, in a vi-kind of fashion
unbind [
bind Escape copy-mode
unbind p
bind p paste-buffer
bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'V' rectangle-toggle
bind-key -t vi-copy 'y' copy-selection
#bind-key -t copy-mode-vi 'v' begin-selection
#bind-key -T copy-mode-vi v send-keys -X begin-selection
#bind-key -T copy-mode-vi V send-keys -X rectangle-toggle
#bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel


# Copy text between tmux and x
#bind y run-shell "tmux show-buffer | xclip -sel clip -i" \; display-message "Copied tmux buffer to system clipboard"

# Move x clipboard into tmux paste buffer
#bind C-p run "tmux set-buffer \"$(xclip -o)\"; tmux paste-buffer"
# bind C-p run "xclip -o | tmux load-buffer - ; tmux paste-buffer"

# Move tmux copy buffer into x clipboard
# bind C-y run "tmux save-buffer - | xclip -i"

# Set status bar
#set -g status-bg black
#set -g status-fg green

#set -g status-left-length 15
#set -g status-left '[ #(hostname | cut -d . -f 1) ]['
#
#set -g status-right '#[fg=yellow]%H:%M #(uptime | cut -d "," -f 2-)'
#set -g status-right '][ %H:%M ]'

#set -g status-right-length 30
#set -g status-right '][ #(uptime | cut -d , -f 4-7 | sed "s/[a-z\:]//g" | sed "s/^[ \t]*//g" | sed "s/, / /g") ][ %H:%M ]'
#set -g status-interval 30

#set -g status-right-length 60
#set -g status-right '][ #(tmux-mem-cpu-load) ][ %H:%M ]'
#set -g status-interval 15

#set -g status-justify centre

# Set statusbar
#setw -g window-status-format '#I #W'
#setw -g window-status-current-format '#[fg=white]( #I #W )'

# Highlight active window
#set-window-option -g window-status-current-bg black
#set-window-option -g window-status-current-fg white

# Automatically set window title
setw -g automatic-rename

# reload tmux conf
# bind-key r source-file ~/.tmux.conf
################################################################################

# This is the end of the part that has been copied from @mosbth

################################################################################

# This is the start of the part of the part that comes from `tmux 2`

################################################################################

# Setting the delay between prefix and command
set -s escape-time 1

# Set the base index for windows to 1 instead of 0
set -g base-index 1

# Set the base index for panes to 1 instead of 0
setw -g pane-base-index 1

# Reload the config file with Prefix r
bind r source $HOME/.tmux.conf \; display "Reloaded!"

# Ensure that we can send Ctrl-A to other apps
bind C-a send-prefix

# Splitting panes with | and -
bind | split-window -h \; display "Splitting horizontally!"
bind - split-window -v \; display "Splitting vertically!"

# Moving between panes with Prefix h,j,k,l
bind h select-pane -L \; display "Selecting pane to the left!"
bind j select-pane -D \; display "Selecting pane below!"
bind k select-pane -U \; display "Selecting pane above!"
bind l select-pane -R \; display "Selecting pane to the right!"

# Quick window selection
bind -r C-h select-window -t:- \; # display "Selecting previous window!"
bind -r C-l select-window -t:+ \; # display "Selecting next window!"

# Resize panes
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# Mouse support - set to on if you want to use the mouse
set -g mouse off

# Set the default terminal mode to 256color mode
set -g default-terminal "screen-256color"

# Set the status line's colors
set -g status-style fg=white,bold,bg=black

# Set the color of the window list
setw -g window-status-style fg=colour4,bg=black

# Set the colors for the active window
setw -g window-status-current-style fg=colour188,bold,bg=colour196

# Set colors for the pane borders
setw -g pane-border-style fg=blue,bg=black
setw -g pane-active-border-style fg=black,bg=colour4

# Set active pane normal, other shaded out
setw -g window-style fg=colour241,bg=colour239    # Non-active pane(s)
setw -g window-active-style fg=white,bg=colour232 # Active pane

# Command / message line
set -g message-style fg=white,bold,bg=black

# Status line left side to show Session:Window:Pane:
set -g status-left-length 60
set -g status-left "#[fg=green]Session: #S. #[fg=cyan]Window: #I. #[fg=yellow]Pane: #P. "

# Status line right side
set -g status-right "#[fg=cyan]%A %d-%B-%Y %R %z %Z"

# Update the status line every sixty seconds
set -g status-interval 60

# Center the window line in the status line
set -g status-justify centre

# Enable activity alerts (showing activity in un-active windows)
setw -g monitor-activity on
set -g visual-activity on

# Set the color of activity alerted windows
setw -g window-status-activity-style fg=white,bg=colour135 #fg is text-color

# Enable vi keys
setw -g mode-keys vi

# Capture pane and save to buffer.txt
# bind C-x capture-pane; save-buffer $HOME/buffer.txt

# Copy current buffer to the system clipboard
bind C-c run "tmux save-buffer - | xclip -i -sel clipboard > /dev/null"

